spring:
  application:
    name: api-gateway
  cloud:
    kubernetes:
      config:
        enable-api: false
        paths: /etc/config/application.yaml
      secrets:
        enable-api: false
        paths: /etc/secret
