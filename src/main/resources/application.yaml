spring:
  application:
    name: api-gateway

management:
  endpoint:
    health:
      probes:
        enabled: true

server:
  port: 8181

gateway:
  test:
    message: "Hello!"
    message2: "World!"
